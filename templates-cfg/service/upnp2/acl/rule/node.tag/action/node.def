type:  txt

help: Rule action [REQUIRED]

syntax:expression: $VAR(@) in "deny", "allow";
                   "action must be one of allow or deny"

allowed: echo "allow deny"

val_help: allow   ; Rule action to allow
val_help: deny    ; Rule action to deny
